[variables]
    PYTHON_VERSION = '3.12'

[phases.setup]
    nixPkgs = ['...', 'python312']

[phases.build]
    cmds = ['pip install -r requirements.txt']

[phases.start]
    cmd = 'python projet_webdev/manage.py migrate && gunicorn --bind 0.0.0.0:8080 projet_webdev.wsgi'
